package testPract;

public class Test {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * String str = "Binu"; String reverse = "";
		 * 
		 * // int strL = str.length();
		 * 
		 * for (int i = str.length() - 2; i >= 0; i--) //for (String string : args) { //
		 * //} { reverse = reverse + str.charAt(i); }
		 * 
		 * System.out.println(reverse);
		 */
//		String name = "ma na";
//		String newName;
//
//		System.out.println(name.replace(" ", "_"));
//	}

	}
}
